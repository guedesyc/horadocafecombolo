// Schema documentation for the D1 bindings. Production changes are applied by drizzle/0000_catalog.sql.
export const schema = { siteContent: 'site_content', adminSessions: 'admin_sessions' } as const;
